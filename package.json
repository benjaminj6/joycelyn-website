{
  "name": "joycelyn-website",
  "version": "1.0.0",
  "description": "Joycelyn Choo's portfolio site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:home && npm run build:about-pages && npm run build:teaching-pages",
    
    "build:home": "assemble --cwd=pages/home",
    
    "build:about-pages": "npm run build:bo && npm run build:teaching-philosophy",
    "build:bio": "assemble --cwd=pages/bio",
    "build:teaching-philosophy": "assemble --cwd=pages/teaching-philosophy",

    "build:teaching-pages": "npm run build:why-private-lessons && npm run build:student-opportunities",
    "build:why-private-lessons": "assemble --cwd=pages/why-private-lessons",
    "build:student-opportunities": "assemble --cwd=pages/student-opportunities"
  },
  "author": "Benjamin Johnson",
  "license": "ISC",
  "dependencies": {
    "font-awesome": "^4.6.3",
    "jquery": "^3.1.0",
    "normalize.css": "^4.2.0",
    "pretty": "^1.0.0"
  },
  "devDependencies": {
    "assemble": "^0.17.1"
  }
}
